<?xml version="1.0" encoding="utf-8" ?>
<configuration>
    <!-- Configuration settings are now stored in the database via ConfigurationService -->
    <!-- No appSettings needed as all configuration is managed dynamically -->
</configuration>
