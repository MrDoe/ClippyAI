<Window xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:ClippyAI.Views"
    xmlns:models="clr-namespace:ClippyAI.Models"
        x:Class="ClippyAI.Views.ConfigurationDialog"
        x:DataType="vm:ConfigurationDialogViewModel"
        x:Name="ConfigurationWindow"
        Title="Configuration"
        Width="900" Height="800"
        WindowStartupLocation="CenterOwner">

    <Grid RowDefinitions="*,Auto">
        <!-- Main content -->
        <TabControl Grid.Row="0" Margin="10">
            
            <!-- General Settings Tab -->
            <TabItem Header="General">
                <ScrollViewer>
                    <StackPanel Margin="10">
                        <Label Content="Ollama Settings" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        
                        <Label Content="Ollama URL:"/>
                        <TextBox Text="{Binding OllamaUrl}" Margin="0,0,0,10" 
                                 ToolTip.Tip="URL for Ollama API"/>
                        
                        <Label Content="Default Model:"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <ComboBox ItemsSource="{Binding ModelItems}" SelectedItem="{Binding OllamaModel}" 
                                      Width="300" ToolTip.Tip="Select a model from the list"/>
                            <Button Content="Pull" Command="{Binding AddModelCommand}" Margin="4,0,0,0"
                                    ToolTip.Tip="Pull/add a model"/>
                            <Button Content="Delete" Command="{Binding DeleteModelCommand}" Margin="4,0,0,0"
                                    ToolTip.Tip="Delete the selected model"/>
                            <Button Content="Refresh" Command="{Binding RefreshModelsCommand}" Margin="4,0,0,0"
                                    ToolTip.Tip="Refresh the model list"/>
                        </StackPanel>
                        
                        <Label Content="Default Language:"/>
                        <ComboBox ItemsSource="{Binding LanguageItems}" SelectedItem="{Binding DefaultLanguage}" 
                                  Margin="0,0,0,10" ToolTip.Tip="Select the default language"/>
                        
                        <CheckBox IsChecked="{Binding AutoMode}" Content="Auto Mode" Margin="0,0,0,10"
                                  ToolTip.Tip="Enable automatic mode"/>
                        
                        <Label Content="Default Task:"/>
                        <ComboBox ItemsSource="{Binding AvailableTasks}" SelectedItem="{Binding DefaultTask}" 
                                  Margin="0,0,0,10" ToolTip.Tip="Select the default task"/>
                        
                        <Label Content="System Prompt:"/>
                        <TextBox Text="{Binding SystemPrompt}" Height="120" TextWrapping="Wrap"
                                 AcceptsReturn="True" Margin="0,0,0,10"
                                 ToolTip.Tip="System prompt for the AI model"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Embeddings Tab -->
            <TabItem Header="Embeddings">
                <ScrollViewer>
                    <StackPanel Margin="10">
                        <Label Content="Vector Database Settings" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        
                        <CheckBox IsChecked="{Binding UseEmbeddings}" Content="Use Embeddings" Margin="0,0,0,10"
                                  ToolTip.Tip="Enable vector database for faster response retrieval"/>
                        
                        <CheckBox IsChecked="{Binding StoreAllResponses}" Content="Store All Responses" Margin="0,0,0,10"
                                  ToolTip.Tip="Store all AI responses as embeddings"/>
                        
                        <Label Content="PostgreSQL Connection String:"/>
                        <TextBox Text="{Binding PostgreSqlConnection}" Height="60" TextWrapping="Wrap"
                                 AcceptsReturn="True" Margin="0,0,0,10"
                                 ToolTip.Tip="PostgreSQL connection string"/>
                        
                        <Label Content="PostgreSQL Ollama URL:"/>
                        <TextBox Text="{Binding PostgresOllamaUrl}" Margin="0,0,0,10"
                                 ToolTip.Tip="Ollama URL accessible from PostgreSQL"/>
                        
                        <Label Content="Embedding Model:"/>
                        <TextBox Text="{Binding EmbeddingModel}" Margin="0,0,0,10"
                                 ToolTip.Tip="Model to use for generating embeddings" 
                                 IsEnabled="{Binding UseEmbeddings}"/>
                        
                        <!-- Threshold -->
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,10,0,0">
                            <Label Content="Threshold:" VerticalAlignment="Center" Width="100"/>
                            <Slider Value="{Binding Threshold}" Minimum="-1" Maximum="1" Width="150" 
                                    TickFrequency="0.1" ToolTip.Tip="Set the threshold value"/>
                            <TextBox Text="{Binding Threshold}" Width="70" Margin="10,0,0,0"/>
                        </StackPanel>
                        
                        <!-- Clear Database Button -->
                        <Button Content="Clear Database" Command="{Binding ClearEmbeddingsCommand}"
                                HorizontalAlignment="Left" Margin="0,10,0,10"
                                ToolTip.Tip="Clear the embeddings database"/>
                        
                        <!-- Embeddings Count -->
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Label Content="# Embeddings:" FontSize="14" VerticalAlignment="Center"/>
                            <Label Content="{Binding EmbeddingsCount}" FontSize="14" VerticalAlignment="Center"
                                   ToolTip.Tip="Displays the number of embeddings"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Vision Tab -->
            <TabItem Header="Vision">
                <ScrollViewer>
                    <StackPanel Margin="10">
                        <Label Content="Vision Settings" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        
                        <Label Content="Vision Model:"/>
                        <TextBox Text="{Binding VisionModel}" Margin="0,0,0,10"
                                 ToolTip.Tip="Model to use for image analysis"/>
                        
                        <Label Content="Video Device:"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <ComboBox ItemsSource="{Binding VideoDevices}" SelectedItem="{Binding VideoDevice}" 
                                      Width="320" ToolTip.Tip="Select the video device from the list"/>
                            <Button Content="Refresh" Command="{Binding RefreshVideoDevicesCommand}" 
                                    Margin="4,0,0,0" ToolTip.Tip="Refresh the list of video devices"/>
                        </StackPanel>
                        
                        <Label Content="Vision Prompt:"/>
                        <TextBox Text="{Binding VisionPrompt}" Height="100" TextWrapping="Wrap"
                                 AcceptsReturn="True" Margin="0,0,0,10"
                                 ToolTip.Tip="Default prompt for image analysis"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- System Tab -->
            <TabItem Header="System">
                <ScrollViewer>
                    <StackPanel Margin="10">
                        <Label Content="System Settings" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        
                        <Label Content="Linux Keyboard Device:"/>
                        <TextBox Text="{Binding LinuxKeyboardDevice}" Margin="0,0,0,10"
                                 ToolTip.Tip="Linux keyboard device for hotkey handling"/>
                        
                        <!-- Configure Hotkey Device Button -->
                        <Button Content="Configure Hotkey Device" Command="{Binding ConfigureHotkeyDeviceCommand}"
                                HorizontalAlignment="Left" Margin="0,0,0,10"
                                ToolTip.Tip="Configure the hotkey device for Linux systems"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- SSH Tab -->
            <TabItem Header="SSH">
                <ScrollViewer>
                    <StackPanel Margin="10">
                        <Label Content="SSH Tunnel Settings" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        
                        <CheckBox IsChecked="{Binding UseSSH}" Content="Enable SSH Tunneling" Margin="0,0,0,10"
                                  ToolTip.Tip="Enable SSH tunneling for secure connections"/>
                        
                        <Label Content="SSH Username:"/>
                        <TextBox Text="{Binding SshUsername}" Margin="0,0,0,10"
                                 ToolTip.Tip="SSH username for authentication"/>
                        
                        <Label Content="SSH Server:"/>
                        <TextBox Text="{Binding SshServerUrl}" Margin="0,0,0,10"
                                 ToolTip.Tip="SSH server hostname or IP address"/>
                        
                        <Label Content="SSH Port:"/>
                        <TextBox Text="{Binding SshPort}" Margin="0,0,0,10"
                                 ToolTip.Tip="SSH server port (default: 22)"/>
                        
                        <Label Content="Local Tunnel:"/>
                        <TextBox Text="{Binding SshLocalTunnel}" Margin="0,0,0,10"
                                 ToolTip.Tip="Local port forwarding (format: local_host:local_port:remote_host:remote_port)"/>
                        
                        <Label Content="Remote Tunnel:"/>
                        <TextBox Text="{Binding SshRemoteTunnel}" Margin="0,0,0,10"
                                 ToolTip.Tip="Remote port forwarding (format: remote_host:remote_port:local_host:local_port)"/>
                        
                        <Label Content="Private Key File:"/>
                        <TextBox Text="{Binding SshPrivateKeyFile}" Margin="0,0,0,10"
                                 ToolTip.Tip="Path to SSH private key file"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Task Configurations Tab -->
            <TabItem Header="Tasks">
                <Grid RowDefinitions="Auto,*,Auto" Margin="10">
                    <!-- Header -->
                    <Label Grid.Row="0" Content="Task-Specific Configurations" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <!-- Main content -->
                    <Grid Grid.Row="1" ColumnDefinitions="250,*">
                        <!-- Left panel - Task list -->
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <Label Content="Tasks:" FontWeight="Bold" Margin="0,0,0,5"/>
                            <ListBox ItemsSource="{Binding TaskConfigurations}" 
                                     SelectedItem="{Binding SelectedTaskConfiguration}"
                                     Height="250" Margin="0,0,0,10">
                                <ListBox.ItemTemplate>
                                    <DataTemplate x:DataType="models:TaskConfiguration">
                                        <TextBlock Text="{Binding TaskName}" />
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            
                            <!-- New task -->
                            <Label Content="New Task Name:"/>
                            <TextBox Text="{Binding NewTaskName}" Margin="0,0,0,5"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <Button Content="Add" Command="{Binding AddNewTaskCommand}" Margin="0,0,5,0" Width="60"/>
                                <Button Content="Delete" Command="{Binding DeleteTaskCommand}" Width="60"/>
                            </StackPanel>

                            <!-- Restore Default Tasks Button -->
                            <Button Content="Restore Default Tasks"
                                    Command="{Binding RestoreDefaultTasksCommand}"
                                    HorizontalAlignment="Left" Margin="0,0,0,0" Width="170"
                                    ToolTip.Tip="Restore the default task configurations"/>
                        </StackPanel>
                        
                        <!-- Right panel - Task details and actions -->
                        <Grid Grid.Column="1" RowDefinitions="*,Auto">
                            <!-- Task details (use ContentControl to avoid null-binding errors) -->
                            <ContentControl Grid.Row="0" Content="{Binding SelectedTaskConfiguration}">
                                <ContentControl.ContentTemplate>
                                    <DataTemplate x:DataType="models:TaskConfiguration">
                                        <ScrollViewer>
                                            <StackPanel>
                                                <Label Content="Task Configuration" FontWeight="Bold" Margin="0,0,0,10"/>
                                                
                                                <Label Content="Task Name:"/>
                                                <TextBox Text="{Binding TaskName}" Margin="0,0,0,10" IsReadOnly="True"/>
                                                
                                                <Label Content="System Prompt:"/>
                                                <TextBox Text="{Binding SystemPrompt}" Height="120" 
                                                         TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,10"/>
                                                
                                                <Label Content="Model:"/>
                                                <ComboBox ItemsSource="{x:Static vm:ConfigurationDialogViewModel.AvailableModels}" 
                                                          SelectedItem="{Binding Model}" 
                                                          Margin="0,0,0,10" Width="300"
                                                          ToolTip.Tip="Select the AI model for this task"/>
                                                
                                                <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto">
                                                    <Label Grid.Row="0" Grid.Column="0" Content="Temperature:"/>
                                                    <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                                                        <Slider Value="{Binding Temperature}" 
                                                                Minimum="0" Maximum="2" Width="100"/>
                                                        <TextBox Text="{Binding Temperature}" Width="60" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                    
                                                    <Label Grid.Row="1" Grid.Column="0" Content="Max Length:" Margin="0,10,0,0"/>
                                                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding MaxLength}" 
                                                             Width="100" HorizontalAlignment="Left" Margin="0,10,0,0"/>
                                                    
                                                    <Label Grid.Row="2" Grid.Column="0" Content="Top P:" Margin="0,10,0,0"/>
                                                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,10,0,0">
                                                        <Slider Value="{Binding TopP}" 
                                                                Minimum="0" Maximum="1" Width="100"/>
                                                        <TextBox Text="{Binding TopP}" Width="60" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                    
                                                    <Label Grid.Row="3" Grid.Column="0" Content="Top K:" Margin="0,10,0,0"/>
                                                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding TopK}" 
                                                             Width="100" HorizontalAlignment="Left" Margin="0,10,0,0"/>
                                                    
                                                    <Label Grid.Row="4" Grid.Column="0" Content="Repeat Penalty:" Margin="0,10,0,0"/>
                                                    <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="0,10,0,0">
                                                        <Slider Value="{Binding RepeatPenalty}" 
                                                                Minimum="0.5" Maximum="2" Width="100"/>
                                                        <TextBox Text="{Binding RepeatPenalty}" Width="60" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                    
                                                    <Label Grid.Row="5" Grid.Column="0" Content="Context Length:" Margin="0,10,0,0"/>
                                                    <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding NumCtx}" 
                                                             Width="100" HorizontalAlignment="Left" Margin="0,10,0,0"/>
                                                </Grid>
                                            </StackPanel>
                                        </ScrollViewer>
                                    </DataTemplate>
                                </ContentControl.ContentTemplate>
                            </ContentControl>

                            <!-- Save task button bound to dialog view model -->
                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,20,0,0">
                                <Button Content="Save Task"
                                        Command="{Binding SaveTaskConfigurationCommand}"
                                        HorizontalAlignment="Left" Margin="0,0,10,0" Width="100"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Name="SaveButton" Content="Save" Margin="0,0,10,0" 
                    Width="80" IsDefault="True"/>
            <Button Name="CancelButton" Content="Cancel" Width="80" 
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>